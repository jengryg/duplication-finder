/**
 * Set the [SIZE_IN_BYTES_FIXED_LENGTH_IN_DECIMAL] to a sufficiently large number of digits, such that the largest
 * directory size in the records can be representing as decimal number without exceeding this limit.
 */
const val SIZE_IN_BYTES_FIXED_LENGTH_IN_DECIMAL = 15